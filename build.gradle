allprojects {
	buildscript {
  		repositories {
    		jcenter()
			}
	}
	repositories {
		jcenter()
	}
	tasks.withType(JavaCompile) {
		options.warnings = false // should be handled in the IDE
	}

    apply from: "$rootDir/buildSrc/versioning.gradle"
}

task hello << {
	println "Hello yatol $version!"
}
